import ComingSoon from '@/components/layout/ComingSoon'

export default function ParentDashboard() {
  return (
    <ComingSoon
      pageLabel="Parent Dashboard"
      pageNumber="08 / 8"
      description="Monitor progress, results, attendance, teacher comments, notifications, and fee payments — the final page in the build order."
    />
  )
}
